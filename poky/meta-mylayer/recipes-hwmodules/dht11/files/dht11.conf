# /etc/dht11.conf — Configurație DHT11 client

# Pinul GPIO la care e conectat DATA al DHT11
# Default: GPIO4 (Pin fizic 7 pe RPi 4B)
GPIO_PIN=4

# Intervalul de trimitere în secunde (minim 2, recomandat >= 30)
# DHT11 nu poate fi citit mai des de o dată la 2 secunde
SEND_INTERVAL=30